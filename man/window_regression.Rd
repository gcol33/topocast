% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/window_regression.R
\name{window_regression}
\alias{window_regression}
\title{Moving-window linear regression over raster matrices}
\usage{
window_regression(y, x, radius, min_cells = 0L, min_variance = 1e-08)
}
\arguments{
\item{y}{Numeric matrix, the response on the coarse grid, or a list of numeric
matrices for several responses sharing the predictors and grid.}

\item{x}{Numeric matrix, or a list of numeric matrices, the predictor(s) on the
same grid as \code{y}.}

\item{radius}{Integer window radius in cells, at least \code{1}; the window is a
square of side \code{2 * radius + 1}. A radius of \code{0} is rejected: its one-cell
window can never hold the \code{k + 1} valid cells a fit needs.}

\item{min_cells}{Integer, additional valid cells required in a window beyond
the \code{k + 1} model terms (\code{k} predictors plus the intercept). Default \code{0}.}

\item{min_variance}{Numeric, the minimum within-window variance a predictor
must have for the cell to be fit. Default \code{1e-8}.}
}
\value{
A list with \code{intercept} (a numeric matrix), \code{slope} (a list of numeric
matrices, one per predictor), \code{r_squared} (a numeric matrix), \code{residual_sd} (a
numeric matrix, the residual standard deviation of the local fit; \code{NA} where
the window has no residual degrees of freedom), and \code{n_valid} (a numeric
matrix, the count of valid cells the window held), each the same size as \code{y}.
\code{n_valid} is shared across responses, since the valid-cell mask is
complete-case across them. When \code{y} is a list of responses, \code{intercept},
\code{r_squared}, and \code{residual_sd} are lists of matrices and \code{slope} is a list of
per-response slope lists, named for the responses.
}
\description{
Fits, for every cell, a linear regression of one or more responses on a shared
set of predictors over a square moving window, and returns full-resolution
intercept and slope grids together with a per-cell coefficient of determination.
The fit uses summed-area tables, so the cost does not grow with the window size.
Cells are excluded from a window where any response or predictor is non-finite; a
cell is returned as \code{NA} when its window holds fewer valid cells than the model
needs or a predictor has no spread. If every cell of every response and predictor
is non-finite, there is nothing to regress and the call errors instead of
returning an all-\code{NA} result.
}
\details{
Several responses share the predictors, so the window design is assembled and
factored once and solved against every response, and each extra response costs
only a back-substitution. The valid-cell mask is therefore complete-case across
the responses: a cell enters a window only where every response and predictor is
finite.

This is the matrix engine behind \code{\link[=topocast]{topocast()}}; it works on plain numeric
matrices and does not depend on terra.
}
\examples{
set.seed(1)
elevation <- matrix(runif(100, 0, 1000), 10, 10)
climate <- 50 + 0.01 * elevation
fit <- window_regression(climate, elevation, radius = 3)
str(fit)

# several responses sharing the predictor: the design is factored once
rain <- 800 - 0.1 * elevation
temp <- 15 - 0.006 * elevation
both <- window_regression(list(rain = rain, temp = temp), elevation, radius = 3)
names(both$slope)

}
\seealso{
\code{\link[=topocast]{topocast()}} for the terra workflow.
}
