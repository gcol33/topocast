% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/window_regression.R
\name{window_regression}
\alias{window_regression}
\title{Moving-window linear regression over raster matrices}
\usage{
window_regression(y, x, radius, min_cells = 0L, min_variance = 1e-08)
}
\arguments{
\item{y}{Numeric matrix, the response on the coarse grid.}

\item{x}{Numeric matrix, or a list of numeric matrices, the predictor(s) on the
same grid as \code{y}.}

\item{radius}{Integer window radius in cells; the window is a square of side
\code{2 * radius + 1}.}

\item{min_cells}{Integer, additional valid cells required in a window beyond
the \code{k + 1} model terms (\code{k} predictors plus the intercept). Default \code{0}.}

\item{min_variance}{Numeric, the minimum within-window variance a predictor
must have for the cell to be fit. Default \code{1e-8}.}
}
\value{
A list with \code{intercept} (a numeric matrix) and \code{slope} (a list of
numeric matrices, one per predictor), each the same size as \code{y}.
}
\description{
Fits, for every cell, a linear regression of the response on one or more
predictors over a square moving window, and returns full-resolution intercept
and slope grids. The fit uses summed-area tables, so the cost does not grow
with the window size. Cells are excluded from a window where the response or
any predictor is non-finite; a cell is returned as \code{NA} when its window holds
fewer valid cells than the model needs or a predictor has no spread.
}
\details{
This is the matrix engine behind \code{\link[=topocast]{topocast()}}; it works on plain numeric
matrices and does not depend on terra.
}
\examples{
set.seed(1)
elevation <- matrix(runif(100, 0, 1000), 10, 10)
climate <- 50 + 0.01 * elevation
fit <- window_regression(climate, elevation, radius = 3)
str(fit)

}
\seealso{
\code{\link[=topocast]{topocast()}} for the terra workflow.
}
